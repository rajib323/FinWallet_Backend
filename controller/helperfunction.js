const ShareList=require('../model/ShareList')


module.exports=async function shareadd(v) {
    /*const axios = require("axios");
    const url = `https://www.google.com/search?q=${v}+share+price`;
    const jsdom = require("jsdom");
    const Share = require('./model/Share');
    const { JSDOM } = jsdom;
    try {
      const { data } = await axios.get(url);
      const dom = new JSDOM(data);
      const d=dom.window.document.querySelector('.BNeawe .iBp4i').textContent.split(' ')
      
      ShareList.create({uin:v,price:parseFloat(d[0])},(err,usr)=>{
        
      })
    } catch (err) {
      console.error(err);
    }*/
  }
